# 前端修订记录

记录日期：2026-09-24

效力：本文件记录已落地的界面修订，供后续把 `docs/05`、前端 EARS、V1.2 冻结文档做总结归档时对照。V1.2 冻结文件（`docs/01`–`04`、产品 `requirements.md` / `design.md` / `tasklist.md`）保持原文。

依据：用户在 F5 预览联调后提出四条改正，已在 `frontend` 分支代码中实现。

## 1. 教师领取状态

预览中曾出现 `已领取 0/6 S1未领、PC2未领…`：内部编号无序拼接，领取后教师屏不刷新。

落地：

- 开放领取或满员后，状态行写 `已领取 x/y`，并分列「已领」「未领」。
- 设备名用中文角色名加编号，顺序为 PC、交换机、路由器、网络分流器。示例：`PC PC1`、`交换机 S1`、`网络分流器 TAP1`。
- 尚未开放领取时只提示课堂已创建，不罗列未领清单。
- 学生 `join` 触发 `classroom.online` 后教师重新拉快照，按 `claimed_connection_id` 更新已领标记。

实现：`web/shared/claim.ts` 的 `formatClaimRoster`；`web/teacher/src/main.ts` 的状态行与快照刷新。

归档时：`docs/05`、教师定员界面需补「领取进度文案」条款。

## 2. 学生端网页标题

浏览器标签原先固定为「纸上谈网 · 学生端」，多标签时无法区分角色与状态。

落地：

- 已领取：`纸上谈网 · {角色中文名} {设备编号}`，例如 `纸上谈网 · 交换机 S1`。
- 未开放领取：`纸上谈网 · 请等待教师确定本课设备`。
- 满员未领到：`纸上谈网 · 本课设备已领完，请看教师屏`。
- 其它：`纸上谈网 ·` 后接当前页提示文案。

实现：`web/shared/claim.ts` 的 `documentTitle`；`web/student/src/main.ts` 每次渲染写入 `document.title`。

归档时：前端 EARS / `docs/05` 学生壳需补浏览器标题规则。

## 3. TAP 中文名

冻结需求与 `docs/01`–`05` 使用「特殊双口交换机」。用户要求界面改用 TAP 的中文名。

落地（仅界面文案，协议与设备 `kind=tap`、编号 `TAP1` 不变）：

- 学生角色名、教师定员步进器：`网络分流器`。
- `ROLE_LABEL.tap = "网络分流器"`。

归档时：总结冻结文档是否把「特殊双口交换机」统一改为「网络分流器」，或注明「文档旧称 / 界面现称」对照。

## 4. 模拟模式数据帧

 原实现用定义列表展示帧字段；目的 PC 在 `frame.arrived` 且 `status=delivered` 时清空帧，只留对话文字。用户要求按教材分栏，并体现打包、解包、改写与到达后的帧+消息。

 落地：

 - 帧形态为并排栏目：目的 MAC、源 MAC、源 IP、目的 IP，下一栏为数据。
 - 源 PC 组帧：先出示「消息」，再「打包：把消息装进数据帧」。
- 交换机：提示按目的 MAC 转发数据帧。
- 路由器：提示「解包查看目的 IP，再重新打包转发」；转发成功且 MAC 被改写时，本机继续展示改写后的帧，变更字段红色。
- 目的 PC：解包后同时保留帧栏目和「消息：…」，对话窗口仍写入收到的汉字。
- 本跳相对上一跳发生变化的 `src_mac` / `dst_mac` / IP / 数据，单元格加 `changed` 并用红色。目的机若 ARP 中源 IP 的 MAC 与帧源 MAC 不同，也标红源 MAC。
- TAP 过路帧列表改用同一套分栏，便于对照。

实现：`web/shared/workbench.ts` 的 `frameCard` / `frameStrip`；`web/shared/claim.ts` 的 `frame.repack`、delivered 时保留 `frame`；`web/student/src/main.ts` 转发成功后用 HTTP 返回帧做改写对比；`web/student/src/style.css` 分栏与红色样式。

归档时：`docs/01` §7 与 `docs/05` 模拟模式画面需补「教材分栏、打包/解包、改写标红、目的机同时显示帧与消息」。冻结接口帧对象字段未改。

## 5. 教师拓扑解除绑定

已领角色不因断线释放。学生 `connection_id` 丢失后无法重进，课堂显示已领完。

落地：

 - 教师拓扑图标右击出现「解除绑定」。
 - `POST /api/v1/classrooms/{id}/devices/{device_id}/unbind`，广播 `claim.released`。
 - 被解绑学生回到等待领取；该角色可被重新领取。满员课堂若有空位则回到 `open`。

实现：`crates/teacher` 的 `unbind_device`；`web/teacher` 右击菜单。

## 6. TAP 挂接后绿灯

教师把网络分流器挂到链路上后，TAP 学生两端端口应对应该链路两端并亮绿灯。

落地：join / hello / `topology.updated` 下发 `tap_attach`；`buildStage` 把 TAP 两口映射到链路 `port_a` / `port_b`，`physicallyUp` 与链路一致。

## 7. 路由器解包再打包分步

模拟模式路由器不再只显示改写后的最终帧。

落地：依次展示「1. 收到数据帧」「2. 网络层」「3. 重新打包的数据帧」。选口前第 3 步提示等待；`frame.repack` 后第 3 步展示改写 MAC，变更字段标红。

## 8. TAP 可多次挂接

挂接成功后挂接框保持可见，允许改挂到另一条已连通链路。后端 `upsert` 覆盖原 `link_id`。

## 9. 对照索引

| 修订 | 冻结/草案中的旧表述 | 代码现状 |
| --- | --- | --- |
| TAP 中文名 | 特殊双口交换机 | 界面：网络分流器 |
| 教师领取进度 | 未写状态行格式 | 已领 x/y + 中文名清单，随 online 刷新 |
| 学生标签标题 | 未写 | `document.title` 随领取/等待/满员变化 |
 | 模拟帧画面 | 展示帧头与数据区；目的机显示消息体 | 分栏帧、打包/解包、改写红色、目的机帧与消息并存 |
 | 教师解绑 | 已领角色不自动释放 | 拓扑右击解除绑定 |
 | TAP 绿灯 | 教师挂接后 TAP 学生无对端 | TAP 两口映射链路两端并亮绿灯 |
 | 路由器分步 | 解包再打包一句带过 | 收到帧 / 网络层 / 重新打包 三步 |
 | TAP 改挂 | 挂接后表单消失、不可再挂 | 表单常驻，upsert 覆盖 |
| 对端手输 | 对端下拉 | 文本框手输端口编号 |
| 高口数底图 | 单张交换机底图 | `port_count>8` 用 `switch-many.svg` |
| 微信风聊天 | 普通对话+ping 表单 | 发起聊天填 IP，失败红底，ping 进对话 |
| PC 舞台 | 网卡底图自带口 | 正面 \| 聊天 \| 背面 RJ45 |
| 端口占用 | 未写占用冲突 | `PORT_BUSY` / 「端口已被占用」 |
| 定员收起 | 定员表常驻 | 开放领取后收起，结束课堂再出现 |
| 拓扑拖动 | 节点固定布局 | 可拖动，连线跟随 |
| 深色指挥台 | 羊皮纸 / 微信绿聊天 | navy 玻璃面板、青绿描边、指挥台聊天 |

后续归档：第 1–7 条与第 8–11 条已并入 `docs/05` 与前端 EARS。产品对外名称若改为网络分流器，再另开授权修订 V1.2 冻结文档。

## 10. 对端端口手输

落地：端口配置里对端为文本框（占位「例如 S1/01」），不再用下拉。空值可拔线。

实现：`web/student/src/stage.ts` 的 `input name="peer_port_id"`。

## 11. 高口数交换机底图

落地：`port_count` 大于 8 时学生端改用 `assets/icons/switch-many.svg`。

实现：`web/shared/stage.ts` 的 `switchChassisKind`；`web/student/src/main.ts` 按口数选底图。

## 12. 微信风聊天

落地：PC 聊天框有「发起聊天」，先填对方 IP；消息发到该 IP。`UNREACHABLE` 时红底「消息发送失败，对方 IP 不可达」。对话框上 ping 当前对象，结果写入对话。

实现：`web/shared/workbench.ts` 的 `chatPanel`；`web/shared/claim.ts` 的 `applyChatError` / `applyPingDetail`；`web/student/src/style.css` 的 `.wx` / `.chat-fail`。

## 13. PC 正反机箱舞台

落地：PC 岗位从左到右为机箱正面、聊天框、机箱背面；背面叠加 1 个 RJ45，坐标 x=63%、y=43%。

实现：`assets/icons/pc-front.svg`、`pc-back.svg`；`web/student/src/stage.ts` 的 PC 三栏舞台。

## 14. 端口占用

落地：目标口已填对端指向他人，或已有其它口指向该目标，保存时 HTTP `409` / `PORT_BUSY` / 「端口已被占用」。原对端可再填同一口。

实现：`crates/teacher/src/classroom.rs` 的 `port_is_busy`；`p2_topology.rs` 的 `occupied_peer_port_is_rejected`。

## 15. 定员窗口收起

落地：领取状态为 open / full 时收起 `#roster`，只留「结束课堂」。结束课堂后定员表再出现。

实现：`web/teacher/src/main.ts` 按 `claiming` 切换；`POST /api/v1/classrooms/{id}/end`。

## 16. 教师拓扑拖动

落地：拓扑节点 `pointerdown` 拖动，连线随 `TopoLayout` 更新。

实现：`web/teacher/src/main.ts` 的 `bindTopoDrag`；`web/shared/topo.ts` 的 `buildTopo` 接受位置覆盖。

## 17. 深色指挥台视觉

落地：教师/学生界面改为深色 navy 指挥台，青绿描边 `#22d3ee`，玻璃面板。教师开放领取后顶栏 HUD + 左侧领取/模式/挂接轨 + 整块拓扑。学生岗位全屏：交换机/路由器机箱做主舞台、表盘在右；PC 正面 | 聊天 | 背面 | 对端。聊天为深色气泡与青描边按钮；`UNREACHABLE` 仍红底「消息发送失败，对方 IP 不可达」。等待/满员为居中玻璃空态。

实现：`web/student/src/style.css`、`web/teacher/src/style.css`；学生 `claimedShell` 增加 `.lab`；教师 `liveDeck` / `draftDeck`。功能与选择器（`#roster` `#end` `.wx` `.topo`）保持不变。

目标画面已保存到 `docs/design/`：`teacher-topo.png`、`student-pc.png`、`student-switch.png`、`student-router.png`、`student-chat.png`、`student-tap.png`。
